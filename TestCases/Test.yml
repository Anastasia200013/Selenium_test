---
TestDescription:
   Test: "Abonent data validation"
   FileAbonent: "C:\\Users\\a.petrova\\PycharmProjects\\selenium-test\\Domain\\abonent.yml"
   Tests:
      - Statement: 'FOR Abonent.Accountcd'
        Type: 'Regex.fullMatch'
        TestSource: 'direct-value'
        Exp: '\d{8}'

      - Statement: 'FOR FIO'
        Type: 'Regex.fullMatch'
        TestSource: 'direct-value'
        Exp: '[А-Яа-я]+( [А-Я].){2}'

      - Statement: 'FOR Address'
        Type: 'Regex.fullMatch'
        TestSource: 'direct-value'
        Exp: '[а-яА-Я0-9,\.\s]+'

       # TestSource: 'SQL'
       # Exp: 'SELECT {Accountcd}'

       # Type: 'Assert.IsTrue'
       # TestSource: 'none'

       # TestDetails:
       # - Statement: 'FOREACH field WHERE Type: string'
       # Type: 'Regex.IsMatch'
       # TestSource: 'direct-value'
       # Exp: '[a-zA-Z\s]{1,100}'

      - Statement: ''
        Type: 'custom'
        TestSource: 'py-script'
        Exp: '/path/to/python/script.py'
       # MSTEst, NUnit, xUnit
       # Regex
...
