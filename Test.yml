---
TestDescription:
   Test: "Abonent data validation"
   FileSelectors: "SearchSelectors.yml"   
   Tests:
     - Statement: 'FOR Accountcd'
       Type: 'Regex.IsMatch'
       TestSource: 'direct-value'
       Exp: '\d{8}'
       
       #TestSource: 'SQL'
       #Exp: 'SELECT {Accountcd}'
       
       #Type: 'Assert.IsTrue'
       #TestSource: 'none'
       
       # TestDetails:         
     - Statement: 'FOREACH field WHERE Type: string'
       Type: 'Regex.IsMatch'
       TestSource: 'direct-value'
       Exp: '[a-zA-Z\s]{1,100}'  
       
     - Statement: ''
       Type: 'custom'
       TestSource: 'py-script'
       Exp: '/path/to/python/script.py'
   #MSTEst, NUnit, xUnit
   #Regex
...
